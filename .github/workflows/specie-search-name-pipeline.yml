name: Pipeline for Specie Search by Name Microservice

on:
  push:
    branches:
      - QA
    paths:
      - 'specie-search-name-service/**'
      - '.github/workflows/specie-search-name-pipeline.yml'
  pull_request:
    branches: 
      - QA
    paths:
      - 'specie-search-name-service/**'
      - '.github/workflows/specie-search-name-pipeline.yml'

jobs:
  build-and-push-docker:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Log in to DockerHub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
      
      - name: Build Docker image
        run: docker buildx build -t ${{ secrets.DOCKER_USERNAME }}/${{ secrets.DOCKER_IMAGE_SPECIE_SEARCH_NAME }}:latest ./specie-search-name-service

      - name: Push Docker image to Dockerhub
        run: docker push ${{ secrets.DOCKER_USERNAME }}/${{ secrets.DOCKER_IMAGE_SPECIE_SEARCH_NAME }}:latest

  deploy-to-ec2-instance:
    needs: build-and-push-docker
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to EC2
        uses: appleboy/ssh-action@v0.1.10
        with:
          host: ${{ secrets.EC2_HOST_TEST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_PRIVATE_KEY }}
          script: |
            echo "Pull, stop, and redeploy the Docker container"
            docker pull ${{ secrets.DOCKER_USERNAME }}/${{ secrets.DOCKER_IMAGE_SPECIE_SEARCH_NAME }}:latest
            docker stop ${{ secrets.DOCKER_IMAGE_SPECIE_SEARCH_NAME }} || true
            docker rm ${{ secrets.DOCKER_IMAGE_SPECIE_SEARCH_NAME }} || true
            docker run --restart always -e MONGO_URI=${{ secrets.MONGO_URI }} \
                       -e DB_NAME=${{ secrets.DB_NAME }} \
                       -e PORT_SEARCH_NAME=${{ secrets.PORT_SEARCH_NAME }} \
                       -d --name ${{ secrets.DOCKER_IMAGE_SPECIE_SEARCH_NAME }} \
                       -p ${{ secrets.PORT_SEARCH_NAME }}:${{ secrets.PORT_SEARCH_NAME }} ${{ secrets.DOCKER_USERNAME }}/${{ secrets.DOCKER_IMAGE_SPECIE_SEARCH_NAME }}:latest
